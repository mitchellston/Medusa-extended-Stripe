{
  "name": "medusa-payment-stripe-extended",
  "version": "1.0.0",
  "description": "Extended Stripe payment provider for Medusa",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/mitchellston/Medusa-extended-Stripe"
  },
  "files": [
    "dist",
    "!dist/**/__tests__",
    "!dist/**/__mocks__",
    "!dist/**/__fixtures__"
  ],
  "engines": {
    "node": ">=20"
  },
  "author": "Mitchellston",
  "license": "MIT",
  "scripts": {
    "test": "jest --passWithNoTests src",
    "build": "bun run rm -rf dist && bun run tsc --build ./tsconfig.json",
    "watch": "bun run tsc --watch",
    "create-release": "bun run build; npm publish"
  },
  "devDependencies": {
    "@medusajs/framework": "2.12.3",
    "jest": "^30.2.0"
  },
  "peerDependencies": {
    "@medusajs/framework": "2.12.3"
  },
  "dependencies": {
    "stripe": "^15.5.0"
  },
  "keywords": [
    "medusa-plugin",
    "medusa-plugin-payment",
    "stripe"
  ]
}
